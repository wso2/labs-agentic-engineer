# Generated BFF OpenAPI spec — a copy of asdlc-service/api/openapi.yaml
# (itself generated by `make openapi`). The console regenerates its typed
# client from it via `npm run gen:api`, so it is not source-controlled.
/openapi.yaml
