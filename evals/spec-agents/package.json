{
  "name": "@aep/spec-agent-evals",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Evaluation framework for the spec-flow agents: per-section (requirements, design, task generation) and chained evals over the real agents service, driven in-process through the playground's production-parity waist. Scenario briefs + rubrics are data (scenarios/*.yaml); scoring is deterministic checks + an LLM rubric judge with pass/review/fail bands.",
  "scripts": {
    "eval": "evalite run",
    "eval:ui": "evalite serve",
    "eval:watch": "evalite watch",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "tsx --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "@aep/agent-stream": "workspace:*",
    "@aep/contracts": "workspace:*",
    "@aep/playground": "workspace:*",
    "@aep/ui-cell-diagram-react": "workspace:*",
    "@ai-sdk/anthropic": "^4.0.0",
    "ai": "^7.0.2",
    "evalite": "^0.19.0",
    "yaml": "^2.6.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.21.0",
    "typescript": "^5.6.3",
    "vitest": "^3.0.0"
  }
}
