prompt: |
  An HTTP alert service that texts every recipient over Twilio and records what
  was delivered per recipient. Raised alerts are kept in memory and fetched by id.

  Generate the service from `openapi.yaml`. Twilio credentials and the sending
  number stay configurable. No tests.

fixtures:
  openapi.yaml: openapi/alerts-twilio.yaml

expect:
  builds: true
  imports:
    - ballerina/http
    - ballerinax/twilio
