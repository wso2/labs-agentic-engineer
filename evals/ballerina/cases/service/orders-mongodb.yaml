prompt: |
  An HTTP order service backed by MongoDB, storing orders in one collection.
  Listing filters by status; a status update on an unknown order is a 404.

  Generate the service from `openapi.yaml`. Connection details stay
  configurable. No tests.

fixtures:
  openapi.yaml: openapi/orders-mongodb.yaml

expect:
  builds: true
  imports:
    - ballerina/http
    - ballerinax/mongodb
