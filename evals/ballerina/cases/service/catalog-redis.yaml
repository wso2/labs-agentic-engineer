prompt: |
  An HTTP catalog facade over an upstream product API, caching each fetched
  product in Redis under a configurable TTL. Search always goes upstream.

  Generate the service from `openapi.yaml`. The upstream url and the Redis
  connection stay configurable. No tests.

fixtures:
  openapi.yaml: openapi/catalog-redis.yaml

expect:
  builds: true
  imports:
    - ballerina/http
    - ballerinax/redis
  importsNot:
    - ballerina/cache
