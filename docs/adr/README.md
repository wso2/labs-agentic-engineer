# Architecture Decision Records

This branch (`feat/dependency-management`) carries **ADR-0007** (P5 platform-resource
provisioning).

Earlier records **ADR-0001 … ADR-0006** — coding-agent placement off the workflow
plane, the local sm-api in-repo stub, coding-agent via cluster-gateway-proxy,
namespaced ComponentType local provisioning, dual-mode OpenChoreo auth /
impersonation, and build git-secret via OpenChoreo — live on the
**`feature/marketplace`** branch. ADR-0007's `[[adr-…]]` links (e.g.
`[[adr-dual-mode-oc-auth-impersonation]]`) resolve there. Bring them over if/when
the branches reconcile.
