# Dependencies
node_modules/

# Build artifacts
dist/

# Go binaries
/asdlc-service/asdlc-service

# Environment files (keep .env.example)
.env
.env.local
.env.*.local

# GitHub App private keys — never commit
deployments/github-app-private-key.pem
*.pem

# Auth-model RS keys generated by setup.sh — never commit
deployments/keys/

# POC API Platform Thunder client creds — generated by setup-thunder-client.sh
deployments/.poc-api-platform-client.env

# Internal kubeconfig — written per start.sh from k3d's --internal view, points
# at the in-cluster server URL so git-service can read it. Re-rendered every
# start; teardown wipes it.
deployments/.kube/

# Test artifacts
test-results/
coverage/
playwright-report/
blob-report/

# Claude & Playwright CLI
.claude/
.playwright-cli/
.playwright-mcp/

# IDE & editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
comm-service/comm-service

.env.bak

.env.backup-pre-oc-rebuild

# deployments-v2 generated files
deployments-v2/.env
deployments-v2/keys/
deployments-v2/.webhook-relay.log
deployments-v2/.webhook-relay.pid
