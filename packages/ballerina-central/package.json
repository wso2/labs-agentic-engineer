{
  "name": "@aep/ballerina-central",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Reads a Ballerina package off the Central docs API and answers four addressed questions about it: an overview, a client's operations by path, one declaration with its dependencies, and the whole API document. Ships as the `bal-library` command the ballerina skill drives.",
  "bin": {
    "bal-library": "./dist/bal-library"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && tsx scripts/bundle.ts",
    "typecheck": "tsc -p tsconfig.check.json",
    "lint": "eslint .",
    "test": "tsx --test \"test/**/*.test.ts\"",
    "record-fixture": "tsx scripts/record-fixture.ts",
    "dev": "echo \"@aep/ballerina-central: no dev server\""
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22",
    "esbuild": "^0.28.1",
    "tsx": "^4.21.0"
  }
}
