<!-- bal-library ops v1 -->
# Operations — ballerina/graphql

| | |
|---|---|
| Source | central |
| Resource functions | none in any client |

`ops` navigates resource functions, which this package does not declare. Its callable surface is remote and normal functions, which `overview` lists in full.

## Clients

- `Client` — 2 remote, 0 normal

## Module-level functions

2, callable without a client.

## Next

- `bal-library ballerina/graphql` — the overview, with every signature
