<!-- bal-library ops v1 -->
# Operations — ballerinax/googleapis.gmail `Client`

| | |
|---|---|
| Source | central |
| Path | `(root)` |

## No operations at this path

Nothing is callable here. Descend to one of the segments below.

## 1 child segment

```
users 32
```

## Next

- descend: `bal-library ops ballerinax/googleapis.gmail 'users'`
- signatures: `bal-library ops ballerinax/googleapis.gmail '(root)' --sigs` — 32 operations, 14,798 bytes
- a declaration named in a signature: `bal-library type ballerinax/googleapis.gmail <Name> [--deps]`
