<!-- bal-library ops v1 -->
# Operations — ballerinax/kafka

| | |
|---|---|
| Source | central |
| Resource functions | none in any client |

`ops` navigates resource functions, which this package does not declare. Its callable surface is remote and normal functions, which `overview` lists in full.

## Clients

- `Caller` — 3 remote, 0 normal
- `Consumer` — 24 remote, 0 normal
- `Producer` — 5 remote, 0 normal

## Next

- `bal-library ballerinax/kafka` — the overview, with every signature
